export const sampleText=`Sample Research Abstract:

We propose a novel attention mechanism for transformer models that reduces computational complexity from O(n²) to O(n log n) while maintaining comparable performance on benchmark tasks. Our approach introduces a hierarchical attention pattern that processes input sequences in multiple scales, allowing the model to capture both local and global dependencies efficiently.

Key contributions include:
1. A novel multi-scale attention architecture
2. Reduced memory footprint during training
3. Competitive results on GLUE and SQuAD benchmarks
4. Faster inference time compared to standard transformers

Experiments demonstrate that our method achieves 94.2% accuracy on SST-2 sentiment classification while using 40% fewer parameters than BERT-base.`
